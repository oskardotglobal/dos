import {type IGameState} from "$/lib/types";
import type {BoardProps} from "boardgame.io/react";

export default function Board(_props: BoardProps<IGameState>) {
    return (
        <div>
            <h1>Board</h1>
        </div>
    );
}